# Copy to .env and fill in. Polygon is optional — without it the scanner falls
# back to yfinance automatically.

POLYGON_API_KEY=

# Optional: override cache + log locations
# CACHE_DIR=data/cache
# LOG_LEVEL=INFO
