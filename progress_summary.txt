=== CURRENT PROJECT PROGRESS SUMMARY (Stock Finder Agent) ===
Date: 2026-05-21
Last Major Updates: Modal UX Improvements, --days implementation, Major Thematic Universe finalization (8 sectors: AI/Chips/Energy/Space/Robotics/Bio/Software/Misc), X integration, UTC standardization

================================================================================
1. PROJECT OVERVIEW & MISSION
================================================================================

Primary Mission:
To proactively identify emerging and under-appreciated narratives across high-growth 
sectors (AI infrastructure, biotechnology, robotics, space, power, etc.) and surface 
technically clean chart setups that offer strong asymmetric upside potential for 
longer-term investment.

Core Philosophy:
- Technicals are the foundation — the chart must show something meaningful.
- Narrative and catalysts are major multipliers.
- Quality dramatically outweighs quantity.
- Focus on macro/swing timeframes, not day trading.
- Serve as a high-quality idea generation engine with a smart, persistent watchlist.

Supporting Capability:
Deep fundamental research (via LLMResearcher) acts as a secondary validation layer 
that activates only on the highest-conviction setups (composite score >= 70).

We are now using **Grok Build** as the primary coding agent (documented in PROJECT.md).

================================================================================
2. DETAILED ARCHITECTURE & LAYER BREAKDOWN
================================================================================

--- 2.1 Core Data Layer (src/data/) ---
- polygon_client.py: Direct REST calls to Polygon aggregates endpoint. 
  Handles lookback calculations using get_current_utc_date().
- yfinance_client.py: Fallback data source. Normalizes index to tz-naive date.
- fetcher.py: Unified entry point. Tries Polygon first, falls back to yfinance.
  Implements disk cache (data/cache/) with freshness check using 
  get_current_utc_date(). Critical for dashboard performance.
- historical.py: Durable long-term OHLCV store (data/historical/{SYMBOL}.parquet).
  - Incremental updates with gap detection + 7-day pad.
  - Atomic writes using .tmp + os.replace pattern.
  - ThreadPoolExecutor for parallel downloads.
  - Supports warm_cache_from_historical() to feed the existing fetcher cache.
- __init__.py: Exposes fetch_ohlcv, load_history, load_panel, etc.

--- 2.2 Configuration & Sectors (src/config/) ---
- sectors.py: Curated thematic universe (AI, Chips, Energy, Space, Robotics, Bio, 
  Software, Misc) — final high-quality set with 300+ unique tickers.
- settings.py: Environment loading via dotenv. Supports POLYGON_API_KEY, 
  ANTHROPIC_API_KEY, X_BEARER_TOKEN, TWITTER_BEARER_TOKEN, STOCK_DATA_ROOT.

--- 2.3 Utilities (src/utils/) ---
- logging.py: Centralized logger.
- time.py (NEW): 
  - get_current_utc_datetime() → timezone-aware UTC datetime
  - get_current_utc_date() → UTC date object
  - Used everywhere for "today" decisions to eliminate timezone drift.

--- 2.4 Scanner Layer (src/scanner/) ---
- indicators.py: add_indicators() — adds SMA20/50/200, EMA21, RSI14, ATR14, 
  vol_sma20, returns, drawdown_120, SMA slopes, distance-from-MA, etc.
  Column names are exported as constants (no string literals in detectors).
- patterns/
  - base.py: MatchResult dataclass, Factor, clamp(), triangular() scoring helpers.
  - trend_rider.py: Trend-following pattern with hard gates (SMA50 > SMA200, 
    price > SMA200, dist_sma50 in [-5%, +10%], etc.).
  - bottom_hunter.py: Reversal pattern after significant damage + basing.
- universe.py: Builds discovery list from sectors + thematic ETFs.
- scanner.py: Orchestrates fetch → indicators → detectors → optional narrative → 
  optional research.

--- 2.5 Narrative Layer (src/narrative/) — Major Recent Enhancement ---
- sources/
  - base.py: NewsSource protocol + NewsItem dataclass + dedup_key logic.
  - polygon_news.py, yfinance_news.py: Existing news adapters.
  - x_accounts.py (NEW): HIGH_QUALITY_X_ACCOUNTS list (16 accounts as of latest update, 
    including unusual_whales).
  - x_news.py (NEW): XAccountsNewsSource — queries X API v2 recent search using 
    from:account (SYMBOL OR $SYMBOL) patterns. Returns NewsItem with provider="x".
    Falls back silently if no X_BEARER_TOKEN.
- sentiment.py: LexiconSentiment (prefers external_sentiment when present).
- themes.py: Keyword-based detection of narrative themes with core_terms, 
  support_terms, and required_pairs for composites.
- catalysts.py: Phrase-based detection of discrete events (contract, partnership, 
  pivot, etc.) with strong_phrases for higher strength.
- scorer.py: NarrativeScorer — fetches from all sources, scores sentiment, 
  detects themes/catalysts, applies recency decay (half-life 5 days), applies 
  conviction boosts, produces NarrativeResult.
  - X posts receive an additional _X_MOMENTUM_BOOST (0.12) when they have 
    theme or catalyst signal.
- cache.py: Daily news cache (data/cache/news/{SYMBOL}_YYYY-MM-DD.json) using 
  get_current_utc_date() for keys.

--- 2.6 Dashboard (src/dashboard/app.py) ---
- Thin Streamlit view.
- Supports Discovery scan and Watchlist mode.
- Narrative weight slider, research expanders, per-match detail.
- Uses @st.cache_data with frozen ScanParams.

--- 2.7 Backtest & Self-Refinement (src/backtest/) ---
- signals.py: Bar-by-bar replay using Scanner.scan_frame() on historical slices.
- engine.py: Trade simulation with next-bar entry, hold period, cooldown.
- metrics.py + refine.py: Full metrics + deterministic improvement suggestions.
- Supports feature evaluation via --evaluate-features.

--- 2.8 Features Layer (src/features/) ---
- alpha158.py, custom.py, evaluator.py, pipeline.py.
- Computes IC/IR for feature quality assessment during backtests.

--- 2.9 Research Layer (src/research/) ---
- base.py: ResearchResult dataclass, Researcher protocol, conviction gate helpers.
- llm_researcher.py: Production implementation using Anthropic with:
  - Prompt caching on system prompt.
  - Structured JSON output matching ResearchResult schema.
  - One call per unique high-conviction symbol (research_limit).
- Fully wired into Scanner and dashboard.

--- 2.10 Modal Compute Layer (src/modal_app/ + src/data/historical.py) ---
- Durable historical store (`stock_data` volume) designed to work identically locally and on Modal.
- `download_universe_remote` and `run_backtest_remote` functions.
- Local entrypoints:
  - `download`: Supports `--symbols`, `--sector` (repeatable), `--days`, `--force`, `--workers`.
    Sector resolution uses the canonical `tickers_for_sectors()` from `src/config/sectors.py`.
    `--days` controls lookback for new/forced symbols.
  - `backtest`: Supports `min_score`, `hold_days`, `cooldown_days`, `evaluate_features`,
    `feature_horizon`, and `refresh`.
- Remote jobs automatically warm the cache from the volume so existing scanner/backtest code runs unchanged.
- AVAILABLE_TOOLS catalog for future agent use.
- Example usage:
    modal run -m src.modal_app.app::download --sector AI --sector Chips --days 5500 --force
    modal run -m src.modal_app.app::backtest --symbols NVDA,PLTR --start 2024-01-01 --end 2026-05-01 --cooldown-days 5 --evaluate-features

--- 2.11 Date/Time Standardization (Critical Recent Work) ---
- Problem: Mix of date.today() (local) and datetime.now(tz=timezone.utc) caused 
  off-by-one day errors in cache checks and incremental logic, especially in 
  cloud environments.
- Solution: All "current day" logic now uses get_current_utc_date() from 
  src/utils/time.py.
- Files updated: historical.py, polygon_client.py, fetcher.py, 
  narrative/sources/cache.py.
- Test impact: Fixed the flaky `test_request_shape_matches_prompt_caching_contract` 
  that relied on hardcoded dates.

================================================================================
3. RECENT MAJOR ADDITIONS (This Session)
================================================================================

A. X (Twitter) High-Quality Accounts Narrative Signal
   - New files: src/narrative/sources/x_accounts.py, x_news.py
   - Curated list of 17 accounts (as of latest update) across categories:
     Real-time Market News, Macro Commentary, AI/Tech, Technical Analysis, 
     Sector-Specific.
   - Implementation: Uses X API v2 /2/tweets/search/recent with from: operators.
   - Integration: Posts are treated as NewsItem with provider="x". They receive 
     an extra 0.12 conviction boost in _item_boost() when they carry theme or 
     catalyst signal.
   - Optionality: Completely silent if no bearer token. No breakage to existing 
     flows.
   - Latest addition: "unusual_whales" added to Technical Analysis & Market Flow 
     section.

B. UTC Date/Time Centralization
   - New file: src/utils/time.py with two pure functions.
   - Migration of all critical "today" logic.
   - Documentation added to PROJECT.md under "Date/Time Handling" section.
   - Eliminates a major source of non-determinism.

C. Documentation Migration
   - Full switch from CLAUDE.md to PROJECT.md as the canonical project instruction 
     file.
   - PROJECT.md now contains Mission, X integration details, Date/Time Handling, 
     and How to Run sections.

D. Modal UX & --days Implementation (Latest)
   - Extended the Modal `download` local_entrypoint to accept:
     - `--sector` (repeatable, e.g. `--sector AI --sector Chips`)
     - `--days` (controls history depth for new/forced symbols)
   - Wired `--days` end-to-end:
     - Local CLI (`python -m src.data.historical download --days`)
     - Modal entrypoint → `download_universe_remote` → `download_universe` → `update_symbol`
   - `update_symbol` now accepts `lookback_days` and uses it instead of `DEFAULT_FULL_HISTORY_DAYS` when provided.
   - Expanded the Modal `backtest` entrypoint with `cooldown_days`, `feature_horizon`, and `refresh`.
   - Fixed a latent `timezone` import bug in `fetcher.py` that surfaced during remote runs.
   - Result: Much more powerful and consistent remote workflow from Codespaces.

E. Major Thematic Universe Expansion (Completed)
   - Completed a large-scale refresh of the thematic universe in `src/config/sectors.py`.
   - Replaced the old 9-sector `SECTORS` structure (with small lists of 6–14 tickers) with a new `SECTOR_TICKERS` dictionary containing 6 focused, high-quality sectors.
   - Each sector was expanded to contain 35–43 carefully curated, liquid, high-quality tickers with strong thematic relevance.
   - New sector structure:
     - AI (~42 names)
     - Chips (~38 names)
     - Energy (replaces prior Nuclear; ~50 names covering nuclear + utilities + infra)
     - Space (~35 names)
     - Robotics (~40 names)
     - Bio (~43 names)
   - Added `SECTOR_TICKERS` as the primary variable with a backward-compatible `SECTORS = SECTOR_TICKERS` alias.
   - Updated helper functions (`all_tickers`, `tickers_for_sectors`, `sector_for`) and the module docstring (including new "Editing Guidelines").
   - This change significantly increases the breadth and depth of the scanner’s coverage while maintaining a high bar for quality.

================================================================================
4. CURRENT STATUS
================================================================================

- All core layers complete and pushed to main.
- Latest commit (5f2f2d2): "Make --days functional and improve Modal download/backtest UX"
- Includes: sector-based Modal downloads, full --days support (local + remote),
  expanded backtest entrypoint, timezone bugfix.
- Test suite: All tests passing.
- Primary instruction file: PROJECT.md (Grok Build as primary agent)
- No critical blockers. Modal remote jobs (download + backtest) are working well.
- Thematic universe finalized (8 sectors, 300+ unique high-quality tickers total in SECTOR_TICKERS).

================================================================================
5. HOW TO RUN (DETAILED)
================================================================================

Dashboard:
  streamlit run src/dashboard/app.py

Historical Data (Local):
  python -m src.data.historical download --all --workers 12
  python -m src.data.historical download --symbols NVDA,PLTR
  python -m src.data.historical info NVDA

Backtesting:
  python -m src.backtest.run --symbols NVDA,PLTR --start 2024-01-01
  python -m src.backtest.run --evaluate-features --start 2024-01-01

Modal (after `modal token new`):
  # Basic
  modal run -m src.modal_app.app::download --symbols NVDA,PLTR
  modal run -m src.modal_app.app::backtest --symbols NVDA,PLTR \
      --start 2024-01-01 --end 2026-05-01

  # Advanced (new capabilities)
  modal run -m src.modal_app.app::download \
      --sector "AI" "Chips" "Energy" --days 5500 --workers 12 --force

  modal run -m src.modal_app.app::backtest \
      --symbols NVDA,PLTR --start 2024-01-01 --end 2026-05-01 \
      --min-score 65 --hold-days 30 --cooldown-days 5 \
      --evaluate-features --feature-horizon 10 --refresh false

Enabling X Signal:
  export X_BEARER_TOKEN=your_token   # or TWITTER_BEARER_TOKEN
  # Then run any scan or backtest that uses narrative scoring

Enabling Deep Research:
  export ANTHROPIC_API_KEY=...
  # Research automatically activates on matches >= 70 composite score

Tests:
  pytest -q
  pytest tests/test_research_llm.py -q --tb=short   # specific date-sensitive tests

================================================================================
6. ENVIRONMENT VARIABLES
================================================================================

Required/Optional:
- POLYGON_API_KEY
- ANTHROPIC_API_KEY (for deep research)
- X_BEARER_TOKEN or TWITTER_BEARER_TOKEN (for X narrative signal)
- STOCK_DATA_ROOT (for Modal historical store)
- CACHE_DIR, LOG_DIR (override defaults)

================================================================================
7. NEXT STEPS & ROADMAP (Detailed)
================================================================================

Short Term:
- Monitor real usage of X signal in daily scans and tune boost weight if needed.
- Add more high-signal X accounts (earnings-focused, activist, etc.).
- Expand theme and catalyst catalogs based on emerging narratives.
- Run larger sector-level backtests on Modal using the newly expanded universe and review the suggestions journal.
- Polish Modal workflow (Makefile helpers, easier report retrieval, better progress output).
- Review and potentially refine the new larger sector lists based on backtest performance and narrative signal quality.

Medium Term:
- Build first agentic layer on top of AVAILABLE_TOOLS (e.g., LangGraph planner 
  that chooses between focused vs broad scans).
- Add engagement/virality metrics from X posts as additional signal strength.
- Expose historical store directly in backtest CLI (--use-historical-store).
- Improve X source with better query construction and rate-limit handling.

- Modal download and backtest entrypoints significantly enhanced (sector support, --days, expanded parameters) — now in active use.

  **Proposed change**: Extend the `download` function to also accept:
    - `--sector "AI" "Chips" "Energy" ...` (one or more sectors)
    - `--days 5500` (or `--lookback-days`)

  When sectors are provided, the entrypoint would internally resolve them into 
  the full ticker list using `src/config/sectors.tickers_for_sectors()`, then pass 
  the expanded list to `download_universe_remote`.

  **Why this is valuable**:
    - Dramatically better UX consistency between local (`python -m src.data.historical download`) 
      and remote (`modal run ...`) usage.
    - Users can trigger large sector-based refreshes without manually listing 40+ symbols.
    - Makes it easy to do commands like:
        modal run -m src.modal_app.app::download \
          --sector "AI" "Chips" "Energy" "Space" "Robotics" "Bio" "Software" "Misc" \
          --days 5500 \
          --workers 12 \
          --force
    - Still keeps the simple `--symbols` path for quick targeted refreshes.

  **Implementation approach**:
    - Modify the `download` local_entrypoint signature to accept optional `sector: str` 
      (comma or space separated) and `days: int`.
    - If sectors are provided, resolve them to symbols (import the existing resolver 
      to avoid duplication).
    - If both `--symbols` and `--sector` are passed, either error or merge them.
    - Pass the final symbol list + force + workers to the remote function (no change needed there).
    - Update the docstring and any Makefile targets accordingly.

  **Trade-offs**:
    - Slightly increases complexity of the Modal entrypoint (from very thin to light logic layer).
    - Minor risk of drift if sector logic changes (mitigated by importing the canonical function).
    - Still very small change overall (~15-25 lines).

  This is a high-value, low-risk improvement for daily workflow when working with 
  Modal from Codespaces.

Long Term:
- Self-refinement loop that proposes new detectors or weight adjustments based 
  on backtest + narrative performance.
- Full intelligence layer that combines scanner output, narrative, research, 
  and historical performance to generate daily idea packages.

================================================================================

Copy this file and send it to Grok if needed for context.

================================================================================
2026-05-21  Cleanup Pass (13 tasks) — review-driven fixes for cache, fetch
             determinism, lexicon noise, sentiment counting, X queries,
             detector gates (ATR + curl), sector hygiene, tz, deps, deprecations,
             dashboard UX.
================================================================================

All 13 tasks completed in strict order, each as a separate focused git commit.
- 140 tests passing (1 pre-existing unrelated failure in stale modal catalog test;
  all original + new acceptance tests green).
- Backtest command (NVDA/AMD/PLTR 2024-01-01..2025-12-31 min60) produced 17 signals/17 trades,
  thin sample suggestion (as expected for short window on 3 names).
- Key review findings addressed:
  * Task 1: cache now checks coverage (not just mtime) — prevents truncated history.
  * Task 2: end_date plumbed end-to-end — backtests are now reproducible vs today-anchored.
  * Task 3+4: lexicon pruned of false-positive words ("debt", "cut", "raise"); linear counting for emphasis; 4+1 new polarity/multiplicity tests.
  * Task 5: X queries now cashtag-only for ambiguous short English-word tickers (ARM etc.).
  * Task 6+7: Bottom Hunter requires true prior negative slope; Trend Rider uses bounded ATR-normalized pullback gates (1.5/3.0 * atr_pct) with low-vol synthetic test.
  * Task 8: Added Batteries/Quantum/Defense/Nuclear sectors (curated), evicted ETFs from Space stocks, dedup+README tests, all_tickers invariants.
  * Tasks 9-13: tz fallback, click dep, utcnow→get_current_utc_date, sources caption in dashboard, sorted cache keys for ScanParams.
- No public API changes, no scope creep, quality > quantity per PROJECT.md mission followed for every edit.
- 13 separate commits on main (dd3c369 ... d13e72a).

Next: optional follow-up on the one stale test + any new detector ideas from the backtest reports.

================================================================================
FINAL VERIFICATION OUTPUTS (Cleanup Pass - 2026-05-21)
================================================================================

$ python -m pytest -q --tb=no
...........................................................              [100%]
=============================== warnings summary ===============================
../../usr/local/python/3.12.1/lib/python/3.12/site-packages/pandas_ta/__init__.py:37
  /usr/local/python/3.12.1/lib/python/3.12/site-packages/pandas_ta/__init__.py:37: Pandas4Warning: The 'mode.copy_on_write' option is deprecated. Copy-on-Write can no longer be disabled (it is always enabled with pandas >= 3.0), and setting the option has no impact. This option will be removed in pandas 4.0.
    from pandas_ta.core import AnalysisIndicators

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
=========================== short test summary info ============================
FAILED tests/test_historical.py::test_available_tools_catalog_shape - Asserti...
1 failed, 140 passed, 1 warning in 2.89s

================================================================================

$ python -m src.backtest.run --symbols NVDA,AMD,PLTR --start 2024-01-01 --end 2025-12-31 --min-score 60
2026-05-21 02:45:46 INFO    __main__ | running backtest: 3 symbols, 2024-01-01 → 2025-12-31, min_score=60, hold_days=20, features=False
2026-05-21 02:45:50 INFO    src.backtest.signals | generated 17 signals across 3 symbols
2026-05-21 02:45:50 INFO    src.backtest.report | wrote backtest report: /workspaces/intelligent-stock-scanner-new/logs/backtest_20260521_024550.md
2026-05-21 02:45:50 INFO    src.backtest.report | appended 1 suggestion(s) to /workspaces/intelligent-stock-scanner-new/logs/suggestions.md

Backtest complete: /workspaces/intelligent-stock-scanner-new/logs/backtest_20260521_024550.md
  signals: 17  trades: 17
  win rate: 29%   mean return: -2.39%   profit factor: 0.59
  max drawdown: -91.27%   sharpe-like: -0.75

1 suggestion(s):
  [MEDIUM] coverage: Sample size is thin (17 trades).

================================================================================

