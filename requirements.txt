# --- core data & indicators ---
pandas>=2.2,<3
numpy>=1.26,<3
pandas-ta>=0.3.14b0

# --- data sources ---
polygon-api-client>=1.14
yfinance>=0.2.40

# --- dashboard ---
streamlit>=1.32
plotly>=5.20

# --- config & utilities ---
python-dotenv>=1.0
requests>=2.31
click>=8.0              # CLI entrypoints (modal_app etc.)
pyarrow>=15.0           # parquet cache backend
tenacity>=8.2           # retry/backoff on API failures
pyyaml>=6.0             # speaker/channel watchlists, narrative configs
feedparser>=6.0         # YouTube + podcast RSS discovery
# Transcript ingestion deps (youtube-transcript-api, yt-dlp, faster-whisper,
# ffmpeg) are declared inline in src/narrative/sources/youtube/modal_app.py
# because they only need to exist inside the Modal ingestion image — they
# add ~1GB and a GPU dependency that the rest of the project doesn't need.

# --- deep research (optional layer) ---
anthropic>=0.40         # LLMResearcher backend

# --- compute layer (optional, opt-in) ---
modal>=0.64             # remote heavy jobs + persistent stock_data volume

# --- dev / test ---
pytest>=8.0
